spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/rentalRooms'
    username: postgres
    password: rootroot
  jpa:
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
  mvc:
    hiddenmethod:
      filter:
        enabled: true

